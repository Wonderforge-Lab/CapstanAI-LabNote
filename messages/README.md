# Messages

Messages are small routed notes between visitor IDs or visitor groups.

Use `templates/message_packet.md`, place open messages in `messages/open/`, and register them in `registry/message_registry.csv`.

Move messages to `answered/`, `closed/`, or `archived/` when their state changes.
