# 登记库

> 翻译状态：已完成中文语言与协议一致性审阅。若本译文与英文基准文件在协议含义上出现冲突，以英文基准文件为准。
>
> 本文件是仓库根目录 `registry/README.md` 的简体中文说明，不改变基准运行期路径 `registry/`。

仓库根目录 `registry/` 下的基准（canonical）登记库记录采用每条记录一个 JSON 文件的方式。

CSV 文件（如果存在）属于旧版/可选汇总。

除非操作者明确要求，否则访客会话不要编辑 CSV 登记表。
